#version: "3.8"
#services:
#  app:
#    image: demo-spring-app
#    container_name: spring-app
#    depends_on:
#      - postgres
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/demo
#      SPRING_DATASOURCE_USERNAME: postgres
#      SPRING_DATASOURCE_PASSWORD: password
#
#  postgres:
#    image: postgres:15
#    container_name: postgres-db
#    environment:
#      POSTGRES_DB: mydb
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: 5692
#    ports:
#      - "5432:5432"
version: "3.8"
services:
  app:
    image: demo-spring-app
    container_name: spring-app
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/demo
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: password

  postgres:
    image: postgres:15
    container_name: postgres-db
    environment:
      POSTGRES_DB: demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
